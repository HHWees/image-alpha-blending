# image-alpha-blending

A small Python project demonstrating alpha blending between two images.

The blended image is computed using:

f(alpha) = alpha * p1 + (1 - alpha) * p2

where alpha ranges from 0 to 1.

The project uses:
- Python
- NumPy
- OpenCV
- Matplotlib
